import React from 'react';
import {Modal} from "antd";

function ResetPasswordModal(isModalOpen, {handleCancel}) {
    console.log(isModalOpen)
    return (
        <div>

        </div>
    );
}

export default ResetPasswordModal;